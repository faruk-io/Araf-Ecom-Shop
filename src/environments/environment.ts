export const environment = {
  production: false,
  // ASP.NET Core dev port from `dotnet run` / Visual Studio's console output —
  // update this whenever the port changes (Visual Studio sometimes reassigns it).
  apiOrigin: 'https://localhost:44368',
  apiBase: 'https://localhost:44368/api'
};
